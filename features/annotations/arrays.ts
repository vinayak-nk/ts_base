const carManufactures: string[] = ['MB', 'BMW', 'TATA', 'Toyota']

// matrix - array of arrays
const matrix: number[][] = []

matrix.push([1])
// matrix.push(1) // error

// mulitple data types in an array

const dates: (Date | string)[] = [new Date(), '2024-09-15']